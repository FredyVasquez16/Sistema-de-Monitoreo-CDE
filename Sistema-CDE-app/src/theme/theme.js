import {createMuiTheme} from "@material-ui/core";

const theme = createMuiTheme({
    palette: {
        primary: {
            light: "#ddb639",
            main: "#d5a408",
            dark: "#957205",
            contrastText: "#ecfad8"
        }
    }
});

export default theme;
